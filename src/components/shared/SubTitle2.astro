---
interface Props {
  className?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { className = "", as = "p" } = Astro.props as Props;
const Tag = as;
---

<Tag
  class={`tracking-wide antialiased
   mb-8 text-lg md:text-4xl title-fw  ${className}`}
>
  <slot />
</Tag>
