---
interface Props {
  className?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { className = "", as = "h1" } = Astro.props as Props;
const Tag = as;
---

<Tag class={`text-6xl title-fw leading-tight mb-4 ${className}`}>
  <slot />
</Tag>
